#In order to run the project, start by installing react-native by following the steps described in https://facebook.github.io/react-native/docs/getting-started.html
#After following the procedure listed in that page, run the following commands in the desired folder:

$react-native init testproj1
$cd AwesomeProject

#move the files contained in this .zip to the folder you just `cd`’d into
$copy /location-to-files/* ./
$react-native run-ios